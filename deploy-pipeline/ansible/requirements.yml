---
# Same pins as estate ansible/requirements.yml (amazon.aws 11.3+).
collections:
  - name: amazon.aws
    version: ">=11.3.0,<12.0.0"
  - name: community.aws
    version: ">=10.0.0,<12.0.0"
  - name: community.docker
    version: ">=4.0.0,<5.0.0"
  - name: ansible.posix
    version: ">=1.6.0,<2.0.0"
