# Activated by @ActiveProfiles("test") on @SpringBootTest classes.
# Profile-specific files overlay main application.properties and are not packaged in the WAR.
# Custom CWE-798 rules exclude src/test; these values are placeholders, not real credentials.
app.jwt.secret=test-jwt-hs256-not-for-production-use!
spring.datasource.password=${POSTGRES_PASSWORD:postgres}
stripe.api.key=${STRIPE_API_KEY:}
stripe.publishable.key=${STRIPE_PUBLISHABLE_KEY:}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:}
